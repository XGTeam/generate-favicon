h3.leading 生成你的favicon.ico

form#upload-favicon-form(name='form')
  .form-group
    button.btn.btn-default(ngf-select, ng-model='file', name='file', ngf-pattern='image/*', accept='image/*', ngf-max-size='20MB') 选择图片

  .form-group
    progressbar.progress-striped.active(ng-if='progressbar.display', value='progressbar.percent', type='info', max='100') {{progressbar.percent}}%

  .form-group
    img.img-responsive(ngf-src='file')

  .form-group
    button(type='submit', class='btn btn-default btn-primary', ng-click='submit()') 生成
